# Phase 0 — Scaffolding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up the empty `portfolio-2026` project with the full dependency tree, base configuration (Vite, TypeScript, Tailwind v4, i18n, router), copied content data, and the project conventions (folder layout, design tokens, attribution) ready for Phase 1.

**Architecture:** A Vite + React 19 + TypeScript project initialized at `/Users/oscarortega/projects/portfolio-2026/`. Tailwind v4 via `@tailwindcss/vite`. Design tokens in CSS variables. i18n with `react-i18next` (en/es). Router with `react-router-dom@7`. Content data copied from the existing `portfolio/` project. The end state is `npm run dev` showing a deep-black page with a cyan `<h1>` placeholder confirming all wiring works.

**Tech Stack:** Vite 6, React 19, TypeScript 5, Tailwind 4, react-router-dom 7, react-i18next, i18next.

**Spec:** `docs/superpowers/specs/2026-05-21-portfolio-2026-design.md`

---

## File map

This phase creates the following files (relative to `/Users/oscarortega/projects/portfolio-2026/`):

**Generated by `npm create vite`:**
- `package.json`
- `vite.config.ts`
- `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`
- `index.html`
- `src/main.tsx`
- `src/App.tsx` (will overwrite)
- `src/vite-env.d.ts`
- `eslint.config.js`
- `.gitignore`

**Hand-written / modified:**
- `vite.config.ts` (modified — add alias + tailwind plugin)
- `tsconfig.app.json` (modified — add path alias)
- `index.html` (modified — title, meta, fonts)
- `src/main.tsx` (modified — i18n init, BrowserRouter)
- `src/App.tsx` (overwritten — routes)
- `src/styles/globals.css`
- `src/styles/tokens.css`
- `src/i18n/index.ts`
- `src/i18n/locales/en.json`
- `src/i18n/locales/es.json`
- `src/data/experiences.json` (copied)
- `src/data/projects.json` (copied)
- `src/data/categories.json` (copied)
- `src/data/skills.ts`
- `src/types/content.ts`
- `src/types/three.ts`
- `src/sections/Home.tsx` (placeholder)
- `src/legal/StickerswapPrivacy.tsx` (placeholder)
- `src/legal/StickerswapTerms.tsx` (placeholder)
- `src/legal/DeleteAccountPage.tsx` (placeholder)
- `src/components/NotFound.tsx`
- `README.md`

---

## Tasks

### Task 1: Initialize the Vite project

- [ ] **Step 1: Confirm the target directory is empty except for `docs/`**

Run:
```bash
ls -la /Users/oscarortega/projects/portfolio-2026/
```
Expected: only `docs/` directory (the spec/plan we wrote already lives here).

- [ ] **Step 2: Initialize Vite with React + SWC + TS template**

Run from inside the project root:
```bash
cd /Users/oscarortega/projects/portfolio-2026 && npm create vite@latest . -- --template react-swc-ts
```
When prompted "Current directory is not empty" → select **"Ignore files and continue"** (the `docs/` folder must be preserved).

Expected: `package.json`, `vite.config.ts`, `tsconfig*.json`, `index.html`, `src/`, `eslint.config.js`, `.gitignore` created. `docs/` untouched.

- [ ] **Step 3: Verify generated files exist**

Run:
```bash
ls /Users/oscarortega/projects/portfolio-2026/
```
Expected output includes: `docs`, `eslint.config.js`, `index.html`, `package.json`, `public`, `src`, `tsconfig.app.json`, `tsconfig.json`, `tsconfig.node.json`, `vite.config.ts`, `.gitignore`.

- [ ] **Step 4: Initialize git and commit the scaffold**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && git init && git add -A && git commit -m "chore: initial Vite scaffold"
```
Expected: initial commit created.

---

### Task 2: Install runtime and dev dependencies

- [ ] **Step 1: Install runtime dependencies**

Run from project root:
```bash
npm install react-router-dom@^7 \
  tailwindcss@^4 @tailwindcss/vite \
  three @react-three/fiber @react-three/drei @react-three/postprocessing \
  gsap lenis framer-motion \
  i18next react-i18next \
  lucide-react zustand
```
Expected: install completes without errors. Verify by reading `package.json` — these entries must appear under `dependencies`.

- [ ] **Step 2: Install dev dependencies**

```bash
npm install -D @types/three prettier vite-plugin-glsl
```
Expected: install completes. Verify entries appear under `devDependencies`.

- [ ] **Step 3: Verify the install is clean**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npm run build
```
Expected: build succeeds (the default Vite template builds even with the new deps just installed but not used yet).

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json && git commit -m "chore: install runtime and dev dependencies"
```

---

### Task 3: Configure Vite, TypeScript path alias, and Tailwind v4

- [ ] **Step 1: Replace `vite.config.ts` content**

File: `/Users/oscarortega/projects/portfolio-2026/vite.config.ts`

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react-swc';
import tailwindcss from '@tailwindcss/vite';
import path from 'node:path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    strictPort: false,
  },
});
```

- [ ] **Step 2: Add path alias to `tsconfig.app.json`**

File: `/Users/oscarortega/projects/portfolio-2026/tsconfig.app.json`

Read the current contents, then add the `baseUrl` and `paths` keys inside `compilerOptions`. Final shape (full file — replace existing):

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"]
}
```

Note: if the generated template differs (different `target`, etc.), preserve the existing values and only add `baseUrl` and `paths`.

- [ ] **Step 3: Verify TypeScript still compiles**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npx tsc --noEmit -p tsconfig.app.json
```
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add vite.config.ts tsconfig.app.json && git commit -m "chore: configure Vite alias and Tailwind v4 plugin"
```

---

### Task 4: Set up design tokens and globals

- [ ] **Step 1: Create `src/styles/tokens.css`**

File: `/Users/oscarortega/projects/portfolio-2026/src/styles/tokens.css`

```css
:root {
  /* Backgrounds */
  --bg-base:   #050a14;
  --bg-elev:   #0a1224;

  /* Cyan palette (primary accent) */
  --cyan-100:  #e0fbff;
  --cyan-300:  #67e8f9;
  --cyan-400:  #22d3ee;
  --cyan-500:  #00d4ff;

  /* Glow / effects */
  --cyan-glow:        0 0 24px rgba(0, 212, 255, 0.45);
  --cyan-glow-strong: 0 0 36px rgba(0, 212, 255, 0.65);

  /* Hologram geometry */
  --hud-corner: 14px;

  /* Easings */
  --ease-hud: cubic-bezier(0.6, 0, 0.1, 1);
  --ease-camera: cubic-bezier(0.45, 0, 0.2, 1);

  /* Z-index layers */
  --z-canvas: 0;
  --z-content: 10;
  --z-cursor: 9000;
  --z-preloader: 9999;
}
```

- [ ] **Step 2: Replace `src/index.css` with `src/styles/globals.css`**

Delete: `/Users/oscarortega/projects/portfolio-2026/src/index.css`

Create: `/Users/oscarortega/projects/portfolio-2026/src/styles/globals.css`

```css
@import "tailwindcss";
@import "./tokens.css";

html, body, #root {
  min-height: 100%;
  background: var(--bg-base);
  color: var(--cyan-100);
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

body {
  margin: 0;
  overflow-x: hidden;
}

* {
  box-sizing: border-box;
}

a {
  color: inherit;
  text-decoration: none;
}

/* Custom scrollbar (cyan, slim) */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: var(--bg-base); }
::-webkit-scrollbar-thumb { background: rgba(0, 212, 255, 0.3); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(0, 212, 255, 0.6); }
```

Delete the default `src/index.css`:
```bash
rm /Users/oscarortega/projects/portfolio-2026/src/index.css
```

(Note: `src/App.css` is NOT deleted yet — the default `src/App.tsx` still imports it. We delete it in Task 8 once `App.tsx` is replaced.)

- [ ] **Step 3: Update the import in `src/main.tsx`**

Open `/Users/oscarortega/projects/portfolio-2026/src/main.tsx`. Replace the import line `import './index.css'` with:

```ts
import './styles/globals.css';
```

The rest of `main.tsx` will be replaced later in Task 7 — leave the rest alone for now.

- [ ] **Step 4: Verify dev server starts**

Run:
```bash
cd /Users/oscarortega/projects/portfolio-2026 && timeout 8 npm run dev || true
```
Expected: server starts, prints "Local: http://localhost:5173/" (or similar). The timeout kills it after 8s — that's fine. No errors in output.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: add design tokens and globals.css"
```

---

### Task 5: Create base type definitions

- [ ] **Step 1: Create `src/types/content.ts`**

File: `/Users/oscarortega/projects/portfolio-2026/src/types/content.ts`

```ts
export type Locale = 'en' | 'es';
export type Localized = Record<Locale, string>;

export type Experience = {
  id: number;
  position: number;
  company: string;
  job_title: Localized;
  description: Localized;
  start_date: string;          // ISO yyyy-mm-dd
  end_date: string | null;
  location: string;
  company_url: string | null;
  technologies: string[];
};

export type Project = {
  id: number;
  title: Localized;
  description: Localized;
  technologies: string[];
  github_url: string | null;
  live_url: string | null;
  app_store_url: string | null;
  play_store_url: string | null;
  image_url: string;
  published: boolean;
  featured: boolean;
  position: number;
};

export type Category = {
  id: number;
  name: Localized;
  slug: string;
};

export type SkillGroup = 'backend' | 'frontend' | 'devops' | 'database';

export type Skill = {
  name: string;
  level: number;            // 0..100
  group: SkillGroup;
  color: string;
};
```

- [ ] **Step 2: Create `src/types/three.ts`**

File: `/Users/oscarortega/projects/portfolio-2026/src/types/three.ts`

```ts
export type SceneKey = 'hero' | 'about' | 'experience' | 'projects' | 'contact';

export type Waypoint = {
  key: SceneKey;
  position: [number, number, number];
  lookAt: [number, number, number];
};
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npx tsc --noEmit -p tsconfig.app.json
```
Expected: no errors. (`noUnusedLocals` may flag unused types, but exported types don't count.)

- [ ] **Step 4: Commit**

```bash
git add src/types/ && git commit -m "feat: add Experience, Project, Category, Skill, Waypoint types"
```

---

### Task 6: Copy content data from the existing portfolio

- [ ] **Step 1: Copy the three JSON files**

```bash
mkdir -p /Users/oscarortega/projects/portfolio-2026/src/data
cp /Users/oscarortega/projects/portfolio/src/data/experiences.json /Users/oscarortega/projects/portfolio-2026/src/data/
cp /Users/oscarortega/projects/portfolio/src/data/projects.json   /Users/oscarortega/projects/portfolio-2026/src/data/
cp /Users/oscarortega/projects/portfolio/src/data/categories.json /Users/oscarortega/projects/portfolio-2026/src/data/
```

- [ ] **Step 2: Verify each file was copied**

```bash
ls /Users/oscarortega/projects/portfolio-2026/src/data/
```
Expected: `categories.json`, `experiences.json`, `projects.json`.

- [ ] **Step 3: Create `src/data/skills.ts` (typed skill list)**

File: `/Users/oscarortega/projects/portfolio-2026/src/data/skills.ts`

```ts
import type { Skill } from '@/types/content';

export const skills: Skill[] = [
  { name: 'Ruby on Rails', level: 95, group: 'backend',  color: '#00d4ff' },
  { name: 'PostgreSQL',    level: 90, group: 'database', color: '#00d4ff' },
  { name: 'React',         level: 85, group: 'frontend', color: '#22d3ee' },
  { name: 'TypeScript',    level: 80, group: 'frontend', color: '#22d3ee' },
  { name: 'GCP',           level: 70, group: 'devops',   color: '#67e8f9' },
  { name: 'Python',        level: 40, group: 'backend',  color: '#67e8f9' },
];
```

- [ ] **Step 4: Verify TypeScript still compiles**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npx tsc --noEmit -p tsconfig.app.json
```
Expected: no errors. The `@/types/content` alias must resolve.

- [ ] **Step 5: Commit**

```bash
git add src/data/ && git commit -m "feat: copy content JSON and add typed skills"
```

---

### Task 7: Set up i18n with en/es

- [ ] **Step 1: Create `src/i18n/locales/en.json`**

File: `/Users/oscarortega/projects/portfolio-2026/src/i18n/locales/en.json`

```json
{
  "hero": {
    "greeting": "HI, I'M",
    "name": "Oscar Ortega",
    "rolePrefix": "> ",
    "roles": ["Full-Stack Engineer", "Ruby on Rails Developer", "React Developer"],
    "cta": "Get in touch",
    "github": "GitHub"
  },
  "about": {
    "title": "About me",
    "description": "Senior backend engineer focused on Ruby on Rails, with hands-on experience across React, TypeScript, and cloud infrastructure.",
    "skillsTitle": "Skills",
    "linkedin": "LinkedIn",
    "contact": "Contact"
  },
  "experience": {
    "title": "Experience",
    "currentLabel": "Present"
  },
  "projects": {
    "title": "Projects",
    "viewLive": "Live demo",
    "viewSource": "Source",
    "appStore": "App Store",
    "playStore": "Google Play"
  },
  "contact": {
    "title": "Get in touch",
    "cta": "Send a message"
  },
  "nav": {
    "hero": "Home",
    "about": "About",
    "experience": "Experience",
    "projects": "Projects",
    "contact": "Contact"
  },
  "footer": {
    "rights": "All rights reserved.",
    "inspired": "Inspired by"
  }
}
```

- [ ] **Step 2: Create `src/i18n/locales/es.json`**

File: `/Users/oscarortega/projects/portfolio-2026/src/i18n/locales/es.json`

```json
{
  "hero": {
    "greeting": "HOLA, SOY",
    "name": "Oscar Ortega",
    "rolePrefix": "> ",
    "roles": ["Ingeniero Full-Stack", "Desarrollador Ruby on Rails", "Desarrollador React"],
    "cta": "Contáctame",
    "github": "GitHub"
  },
  "about": {
    "title": "Sobre mí",
    "description": "Ingeniero backend senior enfocado en Ruby on Rails, con experiencia práctica en React, TypeScript e infraestructura en la nube.",
    "skillsTitle": "Habilidades",
    "linkedin": "LinkedIn",
    "contact": "Contacto"
  },
  "experience": {
    "title": "Experiencia",
    "currentLabel": "Actualidad"
  },
  "projects": {
    "title": "Proyectos",
    "viewLive": "Demo en vivo",
    "viewSource": "Código",
    "appStore": "App Store",
    "playStore": "Google Play"
  },
  "contact": {
    "title": "Hablemos",
    "cta": "Envíame un mensaje"
  },
  "nav": {
    "hero": "Inicio",
    "about": "Sobre mí",
    "experience": "Experiencia",
    "projects": "Proyectos",
    "contact": "Contacto"
  },
  "footer": {
    "rights": "Todos los derechos reservados.",
    "inspired": "Inspirado en"
  }
}
```

- [ ] **Step 3: Create `src/i18n/index.ts`**

File: `/Users/oscarortega/projects/portfolio-2026/src/i18n/index.ts`

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import en from './locales/en.json';
import es from './locales/es.json';

void i18n.use(initReactI18next).init({
  resources: {
    en: { translation: en },
    es: { translation: es },
  },
  lng: typeof navigator !== 'undefined' && navigator.language.startsWith('es') ? 'es' : 'en',
  fallbackLng: 'en',
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **Step 4: Add JSON module resolution to `tsconfig.app.json`**

Open `/Users/oscarortega/projects/portfolio-2026/tsconfig.app.json` and add `"resolveJsonModule": true` inside `compilerOptions`.

- [ ] **Step 5: Verify TypeScript compiles**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npx tsc --noEmit -p tsconfig.app.json
```
Expected: no errors.

- [ ] **Step 6: Commit**

```bash
git add src/i18n/ tsconfig.app.json && git commit -m "feat: set up i18n with en/es locales"
```

---

### Task 8: Set up router with `/` and legal placeholder routes

- [ ] **Step 1: Create the Home placeholder section**

File: `/Users/oscarortega/projects/portfolio-2026/src/sections/Home.tsx`

```tsx
import { useTranslation } from 'react-i18next';

export default function Home() {
  const { t } = useTranslation();

  return (
    <main className="min-h-screen flex items-center justify-center px-4">
      <div className="text-center">
        <p
          className="font-mono uppercase tracking-[4px] text-sm mb-4"
          style={{ color: 'var(--cyan-400)' }}
        >
          {t('hero.greeting')}
        </p>
        <h1
          className="text-5xl md:text-7xl font-bold"
          style={{ color: 'var(--cyan-100)', textShadow: 'var(--cyan-glow)' }}
        >
          portfolio-2026
        </h1>
        <p className="mt-6 font-mono text-xs uppercase tracking-widest" style={{ color: 'var(--cyan-300)' }}>
          Phase 0 — Scaffolding ✓
        </p>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Create the legal placeholder pages**

File: `/Users/oscarortega/projects/portfolio-2026/src/legal/StickerswapPrivacy.tsx`

```tsx
export default function StickerswapPrivacy() {
  return (
    <main className="min-h-screen px-6 py-16 max-w-3xl mx-auto">
      <h1 className="text-3xl font-bold mb-4" style={{ color: 'var(--cyan-100)' }}>
        StickerSwap — Privacy Policy
      </h1>
      <p style={{ color: 'var(--cyan-300)' }}>Content coming in a later phase.</p>
    </main>
  );
}
```

File: `/Users/oscarortega/projects/portfolio-2026/src/legal/StickerswapTerms.tsx`

```tsx
export default function StickerswapTerms() {
  return (
    <main className="min-h-screen px-6 py-16 max-w-3xl mx-auto">
      <h1 className="text-3xl font-bold mb-4" style={{ color: 'var(--cyan-100)' }}>
        StickerSwap — Terms of Service
      </h1>
      <p style={{ color: 'var(--cyan-300)' }}>Content coming in a later phase.</p>
    </main>
  );
}
```

File: `/Users/oscarortega/projects/portfolio-2026/src/legal/DeleteAccountPage.tsx`

```tsx
export default function DeleteAccountPage() {
  return (
    <main className="min-h-screen px-6 py-16 max-w-3xl mx-auto">
      <h1 className="text-3xl font-bold mb-4" style={{ color: 'var(--cyan-100)' }}>
        StickerSwap — Delete Account
      </h1>
      <p style={{ color: 'var(--cyan-300)' }}>Content coming in a later phase.</p>
    </main>
  );
}
```

- [ ] **Step 3: Create the NotFound component**

File: `/Users/oscarortega/projects/portfolio-2026/src/components/NotFound.tsx`

```tsx
import { Link } from 'react-router-dom';

export default function NotFound() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center px-6 text-center">
      <h1 className="text-6xl font-bold mb-4" style={{ color: 'var(--cyan-100)' }}>404</h1>
      <p className="mb-8" style={{ color: 'var(--cyan-300)' }}>Signal lost.</p>
      <Link
        to="/"
        className="px-6 py-3 border rounded font-mono uppercase text-sm tracking-widest"
        style={{ borderColor: 'var(--cyan-400)', color: 'var(--cyan-100)' }}
      >
        Return to base
      </Link>
    </main>
  );
}
```

- [ ] **Step 4: Replace `src/App.tsx` with the route configuration**

File: `/Users/oscarortega/projects/portfolio-2026/src/App.tsx`

```tsx
import { Routes, Route } from 'react-router-dom';
import Home from '@/sections/Home';
import StickerswapPrivacy from '@/legal/StickerswapPrivacy';
import StickerswapTerms from '@/legal/StickerswapTerms';
import DeleteAccountPage from '@/legal/DeleteAccountPage';
import NotFound from '@/components/NotFound';

export default function App() {
  return (
    <Routes>
      <Route path="/" element={<Home />} />
      <Route path="/apps/stickerswap/privacy" element={<StickerswapPrivacy />} />
      <Route path="/apps/stickerswap/terms" element={<StickerswapTerms />} />
      <Route path="/apps/stickerswap/delete-account" element={<DeleteAccountPage />} />
      <Route path="*" element={<NotFound />} />
    </Routes>
  );
}
```

- [ ] **Step 5: Replace `src/main.tsx` to wire i18n + router**

File: `/Users/oscarortega/projects/portfolio-2026/src/main.tsx`

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App';
import './styles/globals.css';
import './i18n';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
);
```

- [ ] **Step 6: Delete the default React assets and `App.css`**

The template ships `src/assets/react.svg` and `src/App.css`, both unreferenced now that we replaced `App.tsx`. Clean them out:
```bash
rm -f /Users/oscarortega/projects/portfolio-2026/src/App.css
rm -f /Users/oscarortega/projects/portfolio-2026/src/assets/react.svg
rmdir /Users/oscarortega/projects/portfolio-2026/src/assets 2>/dev/null || true
```
Expected: no error if a file/dir is already gone.

- [ ] **Step 7: Update `index.html` (title and favicon)**

File: `/Users/oscarortega/projects/portfolio-2026/index.html`

Replace the whole file with:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#050a14" />
    <title>Oscar Ortega — portfolio</title>
    <meta name="description" content="Senior Ruby on Rails engineer. Portfolio of Oscar Ortega." />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

(Favicon will be replaced in Phase 8. `vite.svg` from the template is fine for now.)

- [ ] **Step 8: Run dev server and verify the page**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && timeout 10 npm run dev || true
```
Expected: server starts, no compile errors.

Open `http://localhost:5173/` in a browser. Expected:
- Deep-black background
- Centered: small cyan "HI, I'M" tracking text
- Big "portfolio-2026" heading with a cyan glow
- Small cyan footer text "Phase 0 — Scaffolding ✓"

Test routes manually in the address bar:
- `http://localhost:5173/apps/stickerswap/privacy` → privacy placeholder shows
- `http://localhost:5173/apps/stickerswap/terms` → terms placeholder shows
- `http://localhost:5173/apps/stickerswap/delete-account` → delete-account placeholder shows
- `http://localhost:5173/no-such-route` → 404 page with "Return to base" button

- [ ] **Step 9: Run a production build to catch any regressions**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npm run build
```
Expected: builds successfully into `dist/`.

- [ ] **Step 10: Commit**

```bash
git add -A && git commit -m "feat: wire router with home and legal placeholder routes"
```

---

### Task 9: Write the README with Credits & Inspiration

- [ ] **Step 1: Create `README.md`**

File: `/Users/oscarortega/projects/portfolio-2026/README.md`

```markdown
# portfolio-2026

Personal portfolio site for Oscar Ortega — single-page scroll experience built with React Three Fiber, Tailwind CSS v4, and GSAP-driven scroll animations.

## Scripts

| Command | Description |
| --- | --- |
| `npm run dev` | Start the dev server on port 5173 |
| `npm run build` | Type-check and produce a production build in `dist/` |
| `npm run preview` | Serve the production build locally |
| `npm run lint` | Run ESLint |

## Tech stack

- **Framework:** React 19, TypeScript, Vite
- **Styling:** Tailwind CSS v4 + CSS custom properties (design tokens)
- **3D:** three.js via `@react-three/fiber`, `@react-three/drei`, `@react-three/postprocessing`
- **Animations:** GSAP (+ ScrollTrigger), Lenis (smooth scroll), Framer Motion
- **i18n:** `react-i18next` (en/es)
- **Routing:** `react-router-dom@7`

## Content

All content is mocked in `src/data/` (no backend). Bilingual fields use `{ en, es }` shape:

- `experiences.json` — work history
- `projects.json` — featured projects
- `categories.json` — project categories
- `skills.ts` — typed skills list

## Credits & Inspiration

This portfolio was inspired by [David Heckhoff's portfolio (portfolio-2025)](https://david-hckh.com) — a Vue 3 + three.js project that pioneered the scroll-driven 3D experience pattern used here. portfolio-2026 is an independent re-implementation in the React ecosystem with original 3D assets, custom shaders, and a different visual identity. The original project's design and code are credited to David Heckhoff.
```

- [ ] **Step 2: Commit**

```bash
git add README.md && git commit -m "docs: add README with credits and inspiration"
```

---

### Task 10: Final verification of Phase 0

- [ ] **Step 1: Run a clean build**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && rm -rf dist && npm run build
```
Expected: build succeeds, `dist/index.html` exists.

- [ ] **Step 2: Type-check**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npx tsc --noEmit -p tsconfig.app.json
```
Expected: no errors.

- [ ] **Step 3: Lint**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npm run lint
```
Expected: no errors. (Warnings about unused imports in scaffolded files are acceptable, but the project should not have lint errors.)

- [ ] **Step 4: Run dev one final time and confirm the manual checklist**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && npm run dev
```
Open the URL. Confirm in the browser:

- [ ] Page is deep-black (#050a14), text is cyan
- [ ] Heading reads "portfolio-2026" with a glow
- [ ] Footer line reads "Phase 0 — Scaffolding ✓"
- [ ] Switching browser language to Spanish (or appending `?lng=es` is not wired yet — just confirm en text renders correctly)
- [ ] `/apps/stickerswap/privacy`, `/apps/stickerswap/terms`, `/apps/stickerswap/delete-account` each render their respective placeholder
- [ ] An unknown route renders the 404 page with "Return to base" link going back to `/`
- [ ] No errors in the browser DevTools console

Stop the dev server (Ctrl+C).

- [ ] **Step 5: Confirm git history is clean**

```bash
cd /Users/oscarortega/projects/portfolio-2026 && git log --oneline
```
Expected: a series of commits, the latest being the README commit. No uncommitted changes:
```bash
git status
```
Expected: "nothing to commit, working tree clean".

---

## Phase 0 Deliverable

When all tasks are complete:

- New project at `/Users/oscarortega/projects/portfolio-2026/`
- `npm run dev` shows the placeholder Home page with cyan heading on black
- All 4 base routes work (`/`, 3 legals, and the 404 catch-all)
- i18n is wired (en/es resources loaded, picks locale from browser language)
- Tailwind v4 + design tokens applied
- Path alias `@/*` resolves
- TypeScript strict mode passes
- Production build succeeds
- README documents credits & inspiration to David Heckhoff
- Git history is a clean sequence of focused commits

**Next phase:** `2026-05-21-phase-1-skeleton-and-lenis.md` (to be written after Phase 0 is reviewed).
